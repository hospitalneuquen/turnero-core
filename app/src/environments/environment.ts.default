// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.
const proxyURL = 'http://localhost:1337/api';
// const proxyURL = 'http://172.16.3.15:1337/api';
// const proxyURL = 'http://turnero.hospitalneuquen.org.ar:1337/api';

export const environment = {
    production: false,
    //API: 'http://localhost:1337/api'
     API: 'http://172.16.3.14:1337/api' // last-workin'
    //API: 'http://192.168.56.1:1337/api'
    // API: 'http://turnero.hospitalneuquen.org.ar:1337/api'
};
